# TRR-0009 public capture failure triage addendum

The preserved `failure.json` at `2026-09-07T02:32:21Z` is explained by an adapter dispatch typo, rather than a model, CUDA, or source-input failure. In evaluator HEAD `1f8577d5d32a1268a00600bf3ffdbe9346ca7db4`, `_capture_condition_with_producer` declared the keyword parameter `lora_update_path` but passed `lora_update=lora_update`. Python evaluates that undefined local before entering `trusted._capture_prefix`, producing `NameError: name 'lora_update' is not defined`. The outer wrapper converted it to the generic `CaptureError: public_base public-prefix load failed`; therefore the original 743-byte receipt cannot contain a deeper loader traceback.

The smallest repair is the one committed by root in `1b0e0fa62414bd08c2f2a65d0da7b1e278992ff6`: pass `lora_update=lora_update_path`. The timing-owned failure-diagnostics patch also preserves the exception chain and traceback for any later fail-closed attempt.

Independent read-only evidence supports this diagnosis. The exact trusted `public_base` prefix loader passed on CPU and CUDA with the pinned public snapshot, BF16, and cut depth 4. CUDA evidence is in `prefix_load_diagnostic_v1.json` (PASS, 4.73 s; guard-before-load CUDA free 15,679,356,928 bytes; peak allocated/reserved 2,472,678,400/2,474,639,360 bytes; no observations, source text, labels, or truth). The pinned `model.safetensors` binding is 2,471,645,608 bytes with SHA-256 `1ff795ff6a07e6a68085d206fb84417da2f083f68391c2843cd2b8ac6df8538f`.

A read-only compatibility review of the remaining `_capture_condition_with_producer` calls found no additional obvious signature mismatch: `_qualify_public_prefix_padding(prefix, batches["finance"], device=..., batch_size=8)`, `_live_resource_guard(device)`, `_guard_helpers()`, `capture_public_prefix(..., batch_size=8, resource_check=...)`, the local `save_observation` keyword arguments, and `_enforce_resource_ceiling(device, max_reserved_gpu_bytes=..., max_host_rss_bytes=...)` all match their bound definitions. No fresh capture, source selection, model fit, label/truth access, or prediction run was performed for this diagnosis.
