# Frozen B1 shortlists under target drift

Task: agent3-b1-small-budget-a2. Starting commit 5bbc3bf42a81c814404cf84cb46d55f0d3418667; provenance commit 81299e07d4213cd75f455496d623a8371516688f. This plan is fixed before new source labels or candidate outcomes are inspected.

Use the first 32 records per domain of Agent 2's frozen TRR-P12 order: Pile-like and Finance, 128 positions including BOS and 127 scored. Reuse its one actual target trajectory: unchanged public Llama-3.2-1B-Instruct revision 9213176726f574b556790deb65791e0c5aa438b6; q/v LoRA in prefix layers 0–3, rank 8, alpha 16; seed 6012; 256 updates on separate public Alpaca sources. Stage 64 is the first fitted compatible descendant; stages 128 and 256 are continued adaptation. This is not an externally released full-SFT descendant. All four stages and both domains are mandatory; no outcomes select stages or clips. Agent 2 owns training and observation capture. Its committed manifest at 1031278 governs that process. Reused sources are not an independent replication.

Freeze B1 TRR-0012/expanded_fixed_replication_1 at step 13000: file SHA256 088be6a6b2842d526f3dab39789728d9cfc23f2fb1fb892d107d46ade2382706, normalized public readout ad4201381ec062f0ece1ed007f6a003503e57ef4384271361059f0cc781fdcf1, packaged strict loader and code. Verify bytes and secondary backup. No new fitting, calibration, readout adaptation, routing, fallback, or target-weight access by reconstruction.

Compare historical Alpaca A1 (lens SHA256 33b825dff8eb13cfe877a55bb14e3404c4e3f66355e271fb29004b2d49f4a742) and B1 on exactly the same H/mask/positions. Run FP32 one-record inference, full vocabulary. This is a component port of historical A1's chunk256 scorer to one record (127 rows); qualify against that native function before execution and record numerical differences, if any. Deterministic ranking is score descending, token ID ascending across the entire vocabulary, then retain 256. K=1,8,16,32,64,256 are nested slices. BOS is never scored or inserted in post-BOS candidates. Candidate values, scores, full-score hashes, predictions, numerical settings, code/assets/input bindings, and costs freeze for every cell before the evaluator opens labels. Exact ranks are retained through256 and right-censored above256.

Stage 1 reports domain/stage/K token recall and omissions, exact clip coverage (only a perfect-selector upper bound), rescue among top1 errors, per-record results, paired changes versus stage0 and64, B1-minus-A1 at each K, and B1 small-K minus both B1/A1 K256 references. Use paired source-record bootstrap (seed9013,10000 draws,95% percentile intervals); repeated snapshots do not increase sample size. Report individual snapshots and zero-denominator quantities as null. Bootstrap zero-loss intervals are not equivalence evidence; also give one-sided95% exact upper bound on source-record probability of any loss relative to the reference, including zero observed losses. Boundary change: paired relative L2 and cosine displacement at post-BOS positions versus0 and64.

Prospective acceptable losses: <=0.001 absolute token recall and <=0.02 clip coverage versus both K256 references in every domain and stage. The smallest K among8,16,32 that meets these empirical limits is promising, not established equivalent; uncertainty must be stated. A clearly negative cell rejects that budget for the pilot. No significance test substitutes for equivalence. Stage1 is development if it selects a hybrid budget; held-out P12 second64 sources/domain remain untouched here unless Stage2 is released.

Stage2 is conditional on promising recall AND an existing validated maintained model-prefix recovery path. First audit A2: public model-prefix weights, adapted/recovered model-prefix weights, and own committed token-prefix cache are distinct. If only the existing static public-prefix comparator is available, report the integration gap and finish Stage1; do not build a new recovery framework. A static diagnostic is optional and will not be run merely to fill this gap. If an eligible path exists, use separate held-out sources with common recovered model-prefix snapshots, fixed direct-cosine selector, own causal BOS-started token cache, historicalA1/K256, B1/K256, B1/selectedK and historicalA1/selectedK. Tolerable recovery loss <=0.001 token and <=0.02 exact clips per snapshot/domain; meaningful synchronized total runtime reduction >=20%, including prefix maintenance for tracking claims.

This is a bounded shortlist component study. It does not register a replacement active reconstruction method or claim the required dual-canonical matrix is complete. Any hybrid evaluation must explicitly report missing canonical cells; no overall replacement claim follows from this auxiliary panel.

Compute: no paid compute. Agent1 currently owns GPU. CPU tests use2threads and one-record geometry. Analytical maximum: 127x128256 FP32 logits=65,154,048 bytes; full stable int64 order=130,308,096 bytes; public readout=1,050,673,152 bytes; B1 state~29.4MB. Budget4GiB process RSS and >=8GiB available host RAM, peak guard and 30min cell deadline. GPU, if later used, requires separate lease, measured largest-cell preflight, >=4GiB free beyond estimate and no numerical batching change without equivalence. Preserve failed/excluded attempts, phase timings, actual resources and restart-safe cells. No P03 access; no global STATE.json changes or PR merges. Use task-local state, result and manifest.
