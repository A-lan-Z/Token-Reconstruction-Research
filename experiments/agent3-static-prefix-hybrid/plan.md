# Static-public-prefix B1+A2 pilot — prospective plan

Owner: Agent 3. Task: agent3-static-prefix-hybrid. This task implements the user's relaxation of the maintained-prefix prerequisite. PR25 remains development evidence and is not changed. The base is PR25 commit 3dc1c8d148c002950463fbd96e965cf936e24adc. No new target or decoder fitting is allowed.

## Fixed methods and access

Register these five exploratory arms locally: historical A1 + direct A2 K256; preserved B1 + direct A2 K256; preserved B1 + direct A2 K16; historical A1 + direct A2 K16; preserved B1 rank one alone. The static public Llama-3.2-1B-Instruct prefix, revision 9213176726f574b556790deb65791e0c5aa438b6, embedding and layers 0–3, is common to every A2 arm. Reuse the validated public-prefix loader, native full-vocabulary A1 proposer and native decode_policy. Fixed direct cosine, maximum-score winner (earliest proposer rank breaks exact ties), record batch one, no centering, shortcut, routing, adaptation or abstention. Native candidate microbatch ceiling remains 384, so K256 and K16 each execute one candidate batch per position; no new batching workaround.

B1 is TRR-0012 expanded_fixed_replication_1, step13000, state SHA256 088be6a6b2842d526f3dab39789728d9cfc23f2fb1fb892d107d46ade2382706. Public readout SHA256 ad4201381ec062f0ece1ed007f6a003503e57ef4384271361059f0cc781fdcf1; public A1 lens SHA256 33b825dff8eb13cfe877a55bb14e3404c4e3f66355e271fb29004b2d49f4a742. Verify package dependencies and actual backup bytes. Do not load the newer ordinary-continuation B1.

All proposers use CUDA FP32 with the preserved CPU-normalized fixed readout, highest matmul precision and TF32 disabled; the public prefix uses BF16 SDPA as in P11/P12. B1 GPU execution is a disclosed numerical port of PR25 CPU inference, requiring qualification; no cross-device identity is presumed. All arms use identical inputs and device settings. Each invocation starts a new native cache from BOS, commits only its own winners, and discards its cache on completion. No answers or caches persist across methods, repeats, records or snapshots. Target adapters and source rendering are evaluator-only.

## Qualification before confirmation

Use only PR25 already-opened data: the first source in each domain at base and step256 (four clips). Require: native A1 K256 anchor predictions match the validated uninstrumented adapter; instrumented and uninstrumented native decode outputs match for both budgets/proposers; repeated calls agree; traced commits equal the method's own predicted tokens; every score is finite and all committed tokens belong to its fixed list. Confirm the pinned B1/load/readout identities and compare CPU/GPU B1 proposal outputs without treating equality as assumed. Preserve any differences and fixes before implementation freeze.

Qualify maximum geometry K256 at position127 and retain live CPU/GPU memory and timing evidence before releasing production. Estimate candidate cache upper geometry: 4 layers × 2 K/V × 256 candidates × 128 tokens × 512 KV width × 2 BF16 bytes = 256 MiB, plus model, readout, attention workspaces and allocator overhead. Prior P12 native K256 evidence used approximately 2.24 GiB allocated / 3.31 GiB reserved and 4.84 GiB process RSS. B1/readout coexistence adds a conservatively bounded 1.2 GiB device/host footprint. Initial caps: 8 GiB process CUDA reserved, 12 GiB host RSS, at least 3 GiB free GPU / 6 GiB available host, temperature below83 C. Reassess on anomalies. Use isolated create-only cells and an external fail-closed watchdog. No paid compute.

## Fresh panel and target matrix

After qualification, freeze the implementation and then select a new 32-record panel per domain (64 unique natural sources) using the trusted P11 renderer and latest Agent2 opaque exclusion ledger. All eight conditions use the identical selected 128-position clips: BOS plus127 scored tokens, public base and existing q/v layers0–3 LoRA updates64/128/256. Exclude PR25/P12 sources, B1 fitting/selection, historical opened panels, Agent1/other current reservations, and target fitting/validation sources. A precise hash-bound source-selection contract will be committed before any new reconstruction outcome. Do not use new-panel labels for repairs or parameter changes. Reuse target snapshots and qualified capture profile; do not retrain a target. Capture B8×192 with128 active positions, store BF16 H128/mask/positions. Sanitized observations only enter reconstruction. Freeze all40 method/domain/stage outputs before scoring any new labels.

The fixed method family is exploratory, not a new active canonical replacement. This explicitly bounded owner's matrix does not include the full dual-canonical matrix and cannot establish an overall-best or canonical replacement claim. A retained method is only a static-prefix hybrid candidate within this pilot.

## Prospective quality and acceleration criteria

B1+A2 K16 is empirically acceptable only if, at EACH domain/snapshot, token accuracy loses at most0.1 percentage points and exact-token-clip recovery loses at most2 percentage points versus BOTH historical A1+A2 K256 and B1+A2 K256. Useful acceleration requires at least20% reduction in summed per-record median warmed reconstruction time versus BOTH references in EACH cell (ratio≤0.80). Report every cell even if a late condition fails. These are empirical pilot criteria, not equivalence tests. Report paired source-record bootstrap95% intervals (10,000 resamples, seed9103 for quality,9113 for timing) and exact one-sided95% bounds for any source-record loss; zero-width bootstrap intervals do not prove equivalence. Repeated snapshots and timing repetitions are not independent source samples.

Use the validated timing interface: one warmup and three measured full reconstructions per record; first measured prediction is designated, remaining predictions must match exactly. Proposal is recomputed inside every timed hybrid invocation, replacing the historical proposer rather than adding a second proposal stage. All shared resources are resident; cold loading, preprocessing, artifact I/O, warmups, and diagnostic timing overhead are recorded separately. Rotate arm order deterministically by record to reduce systematic order effects. Coordinate exclusive scientific GPU use with Agent2; record resource samples, host activity and any timing contamination. Preserve fixed workloads and measure actual elapsed time; candidate-count ratios are not speedups.

Instrument native phases without changing returned tensors: proposal, candidate-cache branching plus simulation, direct scoring, own-cache commits, remaining orchestration, whole warmed reconstruction, and peak memory. If detailed instrumentation perturbs outputs, preserve/exclude it and repair only on development data before freeze. Count logical/executed candidate simulations, committed cache tokens and model-prefix calls for all warmup/measured runs and designated predictions separately.

## Evaluation and interpretation

Report token accuracy, token-exact clips, shortlist recall/omissions, wrong selections despite true-token inclusion, and each error type split by whether an earlier wrong token was already committed. Report first-error positions, per-record errors, and correct recoveries after earlier mistakes. 'After an earlier error' is temporal association; without a counterfactual it does not prove causal propagation. B1 alone has proposal errors and no A2 selector. Candidate coverage is only an upper bound. Report each domain/stage and paired changes separately, including B1 alone versus hybrids. Where evaluator tokenizer is available, also report decoded-token-text exactness (distinct from original source-string exactness).

High inclusion with wrong A2 selections under drift motivates investigation of verifier/public-prefix mismatch, not automatic construction of a recovery framework. No active prefix recovery or cold-start tracking claim is permitted. Return this fixed pilot before launching further variants.

## Preservation

Create-only predictions, scores, candidates, commit traces, repeat checks, exact commands, full phase commits, environment/resource receipts and independent actual-byte backups. Keep all new work task-local, no global STATE changes or P03 access. Commit/push task/agent3-static-prefix-hybrid and open an unmerged task PR with result coordination/results/agent3-static-prefix-hybrid.md and manifest experiments/agent3-static-prefix-hybrid/manifest.json.
