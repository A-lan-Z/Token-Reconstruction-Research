BEGIN TRR CONTROL PACKET

PROTOCOL: TRR-RELAY/1.0  
PACKET_TYPE: REVIEW_DISPOSITION_AND_TASK_ASSIGNMENT  
PACKET_ID: TRR-PACKET-TRR-0001-ACCEPT-TRR-0002-20260823-6C3A1F9E84B27D05A93E461C  
REPOSITORY: A-lan-Z/Token-Reconstruction-Research  

PREVIOUS_TASK_ID: TRR-0001  
PREVIOUS_REVISION_ID: TRR-0001-R1  
PREVIOUS_PULL_REQUEST: 2  
PREVIOUS_PULL_REQUEST_BRANCH: task/TRR-0001  
REVIEWED_BASE_COMMIT: 0f641e5f071dd38331d2e2b7821d40fc74941c2e  
PREVIOUS_REVIEWED_HEAD: 2a89a578921afc48dc3b54c65a2c39c35c422134  
R1_PREREGISTRATION_COMMIT: 4169e4444dd99ebdc81a9adbbb9610dd8afc9555  
R1_FIXED_IMPLEMENTATION_COMMIT: 970fdf6c65883bb93878c8458141f1e0bcef3085  
R1_SCIENTIFIC_EVIDENCE_COMMIT: d729e3a614fc3367268eefb4b410b1b5bec4b24c  
FINAL_REVIEWED_HEAD: 1f22e7cfb8d5e25d55ad71fa695beea58463d970  

PREVIOUS_DISPOSITION: ACCEPT_AND_MERGE  
MERGE_AUTHORIZATION: GRANTED_FOR_EXACT_REVIEWED_HEAD_ONLY  
MERGE_REQUIRED_BEFORE_NEXT_TASK: TRUE  

NEXT_TASK_ID: TRR-0002  
NEXT_TASK_BRANCH: task/TRR-0002  
NEXT_TASK_AUTHORIZED: TRUE  

AUTHORITY

RESEARCH_CHARTER.md at the reviewed head remains the sole authoritative definition of the research objective, permitted and unavailable information, causal-state rules, truth-opening requirements, evaluation requirements, and claim scope.

Reviewed charter Git blob:

c446f68e705464ced38d460bac22468e73160f12

Reviewed charter SHA-256:

ab0fbe9dfad39eddee48c14f4cb8201f8c3f02d1c58668d8a8e59be5a250700d

The charter was not modified by TRR-0001 or TRR-0001-R1. 

TRR-0001 REVIEW DISPOSITION

DECISION: ACCEPT_AND_MERGE

Accepted pull request:

#2 — `[TRR-0001] Establish blind reconstruction baselines and cut-depth map`

Accepted exact head:

1f22e7cfb8d5e25d55ad71fa695beea58463d970

At the time of this review, pull request #2 is open, non-draft, mergeable, and unmerged. Its live head exactly matches the supplied commit. 

MEASURED REVIEW FINDINGS

1. REPOSITORY AND REVISION HISTORY

TRR-0001-R1 was appended to the previously reviewed history without amending, rebasing, squashing, or force-pushing it.

The material revision sequence includes:

- clean preregistration and hidden selection commitment at  
  `4169e4444dd99ebdc81a9adbbb9610dd8afc9555`;
- fixed reconstruction implementation at  
  `970fdf6c65883bb93878c8458141f1e0bcef3085`;
- scientific evidence at  
  `d729e3a614fc3367268eefb4b410b1b5bec4b24c`;
- final delivery binding at  
  `1f22e7cfb8d5e25d55ad71fa695beea58463d970`.

coordination/STATE.json correctly records:

- `last_accepted_task: TRR-0000`;
- `active_task: TRR-0001`;
- `revision_id: TRR-0001-R1`;
- branch `task/TRR-0001`;
- pull request #2;
- status `AWAITING_CHATGPT_REVIEW`;
- the original and revision request paths;
- the result and manifest paths;
- the unchanged charter hash.

It contains no self-referential final-head field.

2. ORIGINAL RUN DISPOSITION

The original run at reviewed head  
`2a89a578921afc48dc3b54c65a2c39c35c422134`  
is preserved and explicitly labelled:

`ACCESS_INTERFACE_NONCOMPLIANT_ORIGINAL_RUN`

It is not used as the accepted blind-confirmatory claim basis.

The post-hoc opaque-identifier noninterference audit compared:

- 384 arm-record rows;
- 14,976 candidate lists;
- 239,616 candidate values;
- direct and causal predictions;
- proposal and selection scores;
- query tensors;
- method state;
- routing and stopping;
- operation counts.

Those algorithmic outputs were unchanged when source-derived identifiers were replaced with opaque identifiers. This audit supports the narrow conclusion that the original implementation did not, in fact, branch on those identifiers. It does not retroactively repair the original access interface, and the result correctly preserves that distinction.  

3. CLEAN PREREGISTRATION AND HIDDEN RECORD SELECTION

The R1 plan and public selection commitment were committed before the clean blind reconstruction.

The public commitment:

- identifies the pinned public dataset and revision;
- contains 64 opaque record IDs;
- does not disclose dataset row indices, source hashes, plaintext, token IDs, or the selection key;
- commits through HMAC-SHA256 to the evaluator-private ordered mapping;
- declares that mapping and key revelation occurs only after verified freeze.

The exact commitment present at the preregistration commit is:

7f459452a1a42525e18baafd1a8ed5eb1d4993d6c37fe00a6bed401a80e6fe7c

The post-freeze reveal verification establishes:

- the HMAC commitment matched;
- all 64 records were eligible;
- opaque order matched;
- the fresh records were disjoint from the 288 records used by the original target-update, inverse-training, development, and blind splits;
- the selection algorithm reproduced the revealed records;
- no token IDs were disclosed during mapping reveal;
- the truth sidecar was not read during reveal verification.

The reveal was created only after the freeze receipt and both access manifests were reverified.   

4. SANITIZED RECONSTRUCTION INTERFACE

The clean reconstructor received opaque IDs of the form:

`blind-r1-000001` through `blind-r1-000064`

The sanitized configuration and observation index exclude:

- dataset indices;
- source-text hashes or prefixes;
- source-token hashes;
- source-derived record IDs;
- source plaintext;
- source token IDs;
- the hidden selection key;
- the private mapping;
- target-LoRA paths or tensors;
- truth or correctness feedback.

The schema validators reject prohibited or unexpected source-resolving metadata rather than merely ignoring it.

5. FAIL-CLOSED RECONSTRUCTION ACCESS BOUNDARY

Direct and causal reconstruction ran as separate sequential processes.

Each process used:

- a fresh Linux user namespace;
- a fresh mount namespace;
- a fresh network namespace;
- a fresh PID namespace;
- `pivot_root` into a minimal staged root;
- read-only code, model, runtime, inverse-state, and sanitized-input mounts;
- writable locations limited to the method output, `/tmp`, and `/dev/shm`;
- a new `/proc`;
- no host workspace mount after `pivot_root`;
- no default network route;
- only loopback visible;
- a clean allowlisted environment.

Each process passed denial probes for:

- the repository workspace;
- the evaluator-private fresh mapping;
- the clean truth sidecar;
- the original truth sidecar;
- the target LoRA;
- the dataset cache;
- outbound IPv4 connectivity.

The resulting access manifests were validated before reconstruction inputs were loaded and were themselves included in the frozen bundle.   

6. FIXED SCIENTIFIC METHODS AND STATE

The clean confirmatory run held the intended scientific comparison fixed.

It reused the exact retained state identities:

- target LoRA SHA-256:  
  `34d92f1e664236bfa1990b10148e8ad52c60b16e72ed0ff4c7eb7da8d15019f6`;
- cut-4 inverse SHA-256:  
  `9e2487f85057748130bf87b2aad0a883f3c36dfc052eefd83c0f5c35497a24e3`;
- cut-8 inverse SHA-256:  
  `ac8871f1fa0d40664c5d9d94343ef560832477aade3574978a1c6b572df01e80`.

It used the same:

- pinned model and tokenizer;
- public dataset revision;
- target-prefix update;
- inverse states;
- sequence geometry;
- cuts 0, 4, and 8;
- matched-public and unavailable-target conditions;
- candidate budget 16;
- direct inverse;
- causal public-surrogate activation-matching selector;
- BOS and reconstructed-prefix semantics;
- stopping and no-abstention rules;
- record-bootstrap procedure.

The reconstruction processes performed:

- zero fresh training steps;
- zero fresh adaptation steps;
- zero target-prefix calls.

Candidate IDs, proposal scores, and query tensors recomputed independently in the direct and causal processes were identical before the causal selector was applied.

7. FREEZE AND TRUTH-OPENING ORDER

The clean freeze receipt was created at:

2026-08-22T13:01:17Z

It covers 22 entries and 364,872,246 bytes, including:

- the sanitized reconstruction-input bundle;
- exact inverse states;
- both method-specific access manifests;
- direct and causal predictions;
- candidate lists and scores;
- query tensors;
- routes and stopping decisions;
- method timing and memory records;
- runtime identity;
- code and model identities;
- pre-freeze validation.

Its SHA-256 is:

72792c734b4ca89f7afe660d2cd2da1d61dd49342901a5a527a85576ec3b4845

The receipt records:

- `selection_revealed: false`;
- `truth_opened: false`;
- `access_manifests_verified: true`.

The source mapping was revealed and verified afterward at approximately 13:06 UTC.

Scoring began at 13:09:46 UTC. Its first phase reverified:

- the freeze receipt;
- both access manifests;
- the sanitized interface;
- the mapping reveal;
- the reveal-verification record.

Only after those checks succeeded did the scorer read the truth sidecar.  

8. CLEAN CONFIRMATORY QUALITY RESULTS

Each arm contains:

- 64 fresh blind records;
- 39 scored non-BOS tokens per record;
- 2,496 scored tokens.

The clean results are:

| Condition | Cut | Direct accuracy | Causal accuracy | Causal − direct | 95% paired record CI | Top-16 recall | Conditional causal accuracy |
|---|---:|---:|---:|---:|---:|---:|---:|
| Matched public | 0 | 100.00% | 100.00% | 0.00 pp | [0.00, 0.00] pp | 100.00% | 100.00% |
| Matched public | 4 | 62.90% | 81.93% | +19.03 pp | [+16.99, +21.23] pp | 82.97% | 98.74% |
| Matched public | 8 | 48.88% | 67.11% | +18.23 pp | [+15.42, +20.95] pp | 72.72% | 92.29% |
| Unavailable target | 0 | 100.00% | 100.00% | 0.00 pp | [0.00, 0.00] pp | 100.00% | 100.00% |
| Unavailable target | 4 | 64.70% | 83.97% | +19.27 pp | [+17.19, +21.43] pp | 84.66% | 99.20% |
| Unavailable target | 8 | 49.68% | 67.31% | +17.63 pp | [+15.10, +20.19] pp | 73.08% | 92.11% |

The exact primary values are:

- direct inverse:  
  `0.6470352564102564`;
- causal public-surrogate search:  
  `0.8397435897435898`;
- paired causal-minus-direct effect:  
  `0.19270833333333331`;
- 95% record-bootstrap interval:  
  `[0.17187499999999997, 0.21434294871794868]`.

The primary interval excludes zero.

No method produced an exact 39-token sequence at cuts 4 or 8. Cut 0 reconstructed exactly.

At the primary cut:

- frozen top-16 recall was 84.66%;
- causal accuracy conditional on true-token inclusion was 99.20%;
- teacher-prefix counterfactual accuracy was 84.66%;
- frozen reconstructed-prefix causal accuracy was 83.97%.

At cut 8:

- frozen top-16 recall was 73.08%;
- conditional causal accuracy was 92.11%;
- teacher-prefix counterfactual accuracy was 72.52%;
- frozen reconstructed-prefix causal accuracy was 67.31%.

These measurements replicate the original run’s qualitative result on a fresh, access-compliant split.   

9. METHOD-SPECIFIC COST EVIDENCE

Direct and causal methods were measured in separate equivalent processes with method-phase CUDA counters reset.

Across all six condition/cut arms:

| Measure | Direct | Causal |
|---|---:|---:|
| Proposal time | 1.2782 s | 1.2766 s |
| Selection/simulation time | 0.0000 s | 8.8985 s |
| Total measured method compute | 1.4365 s | 10.3291 s |
| Mean per record-arm | 0.0037408 s | 0.0268988 s |
| Mean per scored token | 0.00009592 s | 0.00068971 s |
| Method peak CUDA allocated | 3,688,399,872 B | 4,071,908,352 B |
| Method peak CUDA reserved | 5,368,709,120 B | 5,368,709,120 B |
| Process peak RSS | 3,386,292 KiB | 3,386,252 KiB |

Causal method compute was approximately 7.19 times direct method compute.

The causal method therefore establishes a quality improvement at a shared candidate budget, not a Pareto improvement.

10. TESTS AND FINAL VALIDATION

The committed final focused suite reports:

17 passed in 1.68 seconds. 

The committed final full source-checkout suite reports:

35 passed in 2.00 seconds. 

The post-manifest validator reports:

`PASS_ALL_R1_INTEGRITY_CHECKS`

It confirms:

- 384 direct rows;
- 384 causal rows;
- 768 metric rows;
- zero metric inconsistencies;
- six query arms with shape 64 × 39 × 2,048;
- zero target-prefix calls;
- zero network default routes;
- exact target and inverse-state hashes;
- 64 truth records opened only after all gates. 

The final tracked-JSON pass reports:

`PASS_TRACKED_JSON 35`. 

SCIENTIFIC INTERPRETATION

The clean evidence supports the following decisions within the exact evaluated scope:

1. Causal public-surrogate candidate selection improves token accuracy over the direct inverse at cuts 4 and 8 under candidate budget 16.

2. Recoverability declines materially from cut 4 to cut 8 for both methods.

3. Candidate proposal recall is the dominant measured bottleneck at the primary cut. Conditional selector accuracy is already approximately 99.2%, and replacing reconstructed prefixes with true prefixes closes less than one percentage point.

4. Prefix error propagation becomes more material at cut 8, but proposal recall remains the larger immediate ceiling there.

5. Reliable exact full-sequence recovery at non-embedding cuts is not established.

6. The single retained target LoRA does not establish a general unavailable-prefix mismatch penalty. Its unavailable-target condition was not consistently worse than the matched-public control.

7. The causal method’s quality gain carries substantial compute and implementation cost and is not a Pareto improvement.

The current ROADMAP.md and DECISIONS.md accurately distinguish the accepted clean evidence from the noncompliant original run and identify candidate proposal as the highest-value next direction without treating that direction as a permanent method restriction.  

NON-BLOCKING EVIDENCE QUALIFICATION

The R1 plan describes both the dataset package and dataset cache as absent from the reconstruction chroot.

The committed access evidence directly demonstrates absence of:

- the dataset cache;
- the source mapping;
- the repository workspace;
- truth;
- target state;
- network access.

The launcher mounts a declared read-only Python site-packages tree. The evidence therefore does not independently prove that every dataset-related Python module was absent from that tree.

This wording is broader than the direct probe evidence. It does not invalidate the run because:

- no dataset rows or cache were mounted;
- no source mapping was mounted;
- network access was unavailable;
- the reconstructor did not import or invoke dataset-loading code;
- the opaque current records could not be resolved from the exposed inputs.

Future access manifests should distinguish precisely between:

- absence of dataset content and caches;
- absence of current-record mappings;
- and presence or absence of general-purpose runtime packages.

No additional TRR-0001 revision is warranted for this documentation qualification.

MERGE INSTRUCTIONS

1. Re-fetch pull request #2 immediately before merging.

2. Verify that its exact live head remains:

   `1f22e7cfb8d5e25d55ad71fa695beea58463d970`

3. If the live head differs, do not merge the unreviewed commit. Record the mismatch and stop.

4. If the head matches, merge pull request #2 using a merge commit.

5. Do not squash, rebase, amend, or otherwise replace the reviewed commit chain.

6. Fetch the updated main branch and record:

   - the exact merge command or GitHub operation;
   - merge method;
   - exit status;
   - resulting main commit;
   - merged pull-request state;
   - confirmation that  
     `1f22e7cfb8d5e25d55ad71fa695beea58463d970`  
     is an ancestor of the resulting main.

7. Create `task/TRR-0002` from that updated main only after the merge is complete.

NEXT TASK

TASK_ID: TRR-0002  
TITLE: Improve blind candidate proposal and map the proposal–selection quality-cost frontier  
TASK_TYPE: SUBSTANTIVE_RESEARCH  

RESEARCH QUESTION

Can a stronger charter-compliant, public-resource candidate proposer increase frozen true-token recall at cuts 4 and 8 sufficiently to improve end-to-end causal reconstruction accuracy, and what runtime, memory, state, and candidate-simulation costs are required to obtain those gains across multiple unavailable-prefix updates?

DECISION ENABLED

TRR-0002 must determine:

- whether investment in candidate proposal produces a reproducible end-to-end gain;
- which proposal family and candidate budget lie on the measured quality-cost frontier;
- whether proposal improvements remain consistent across multiple unavailable target-prefix updates;
- whether cut-8 performance remains proposal-limited or becomes primarily limited by causal ranking and prefix propagation;
- whether the next task should continue improving proposal, reduce candidate-scoring cost, improve deeper-cut causal robustness, or change reconstruction family.

WHY THIS TASK HAS HIGHEST VALUE NOW

At the accepted primary cut:

- causal accuracy is 83.97%;
- top-16 recall is 84.66%;
- conditional causal selection accuracy is 99.20%;
- the true-prefix counterfactual reaches 84.66%.

The fixed selector therefore recovers nearly every true token it is offered. Most remaining cut-4 errors arise because the true token is absent from the frozen proposal set.

At cut 8:

- causal accuracy is 67.31%;
- top-16 recall is 73.08%;
- conditional selection accuracy is 92.11%.

Proposal remains the largest immediate ceiling, although ranking and prefix propagation are no longer negligible.

The accepted evidence is from one target-update instance. A proposal method should not be promoted based on that single update alone.

HYPOTHESES

Treat these as testable hypotheses, not assumptions or permanent constraints:

H1. A context-aware or otherwise stronger public-resource proposer can improve top-16 recall over the accepted residual-affine proposer at cuts 4 and 8.

H2. At cut 4, proposal-recall gains will convert nearly one-for-one into end-to-end causal token-accuracy gains because the accepted selector is already nearly perfect conditional on inclusion.

H3. Increasing candidate budget will improve reconstruction quality with diminishing returns and approximately linear selector cost.

H4. At cut 8, proposal gains will convert less completely because conditional ranking and reconstructed-prefix errors are more material.

H5. A real proposal improvement will remain directionally consistent across multiple independently constructed unavailable-prefix updates.

TEMPORARY TASK SCOPE

The requirements below define TRR-0002’s comparison. They are not permanent restrictions on future methods.

MODEL, TOKENIZER, AND DATASET

Use:

- `meta-llama/Llama-3.2-1B-Instruct`
- revision  
  `9213176726f574b556790deb65791e0c5aa438b6`
- `NeelNanda/pile-10k`
- revision  
  `127bfedcd5047750df5ccf3a12979a47bfa0bafa`
- 39 scored source tokens after the declared BOS token.

CUTS

Use:

- cut 4 as the primary cut;
- cut 8 as the depth-stress cut.

A small cut-0 semantic regression check is required, but the full candidate-budget frontier need not be rerun at cut 0 unless Codex identifies a scientific reason.

ACCEPTED REFERENCE PROPOSER

Retain the accepted TRR-0001 residual-affine proposer as the compatibility baseline.

Use the exact accepted inverse states:

- cut 4:  
  `9e2487f85057748130bf87b2aad0a883f3c36dfc052eefd83c0f5c35497a24e3`;
- cut 8:  
  `ac8871f1fa0d40664c5d9d94343ef560832477aade3574978a1c6b572df01e80`.

Do not silently retrain or replace these states when reporting compatibility-baseline results.

A retrained or resource-matched affine variant may be included as an additional comparison, but it must be labelled separately.

FIXED CAUSAL SELECTOR

Hold the accepted selector semantics fixed:

- frozen candidate list;
- pinned public surrogate;
- declared BOS;
- only already committed reconstructed tokens;
- public-prefix candidate simulation;
- activation matching at the declared cut;
- deterministic tie-breaking;
- winner-only causal commit;
- zero unavailable-target-prefix calls;
- no correctness feedback.

Changes needed solely to support a variable candidate count or improve implementation efficiency are permitted if semantic-equivalence tests pass and the deviation is recorded.

PROPOSAL CHALLENGERS

Develop and compare at least one materially stronger proposal family against the accepted residual-affine proposer.

Eligible approaches include, but are not limited to:

- nonlinear inverse models;
- reconstructed-prefix-conditioned proposal;
- activation and public-language-prior fusion;
- ensembles or unions of independently trained public inverses;
- multi-position or sequence-context proposal;
- public-surrogate activation retrieval;
- any other charter-compliant method.

Codex may select a stronger approach.

Do not prescribe a method merely because it appears in this packet’s examples.

Any training or model selection must use only declared public, auxiliary, prior-completed-record, or development information permitted by the charter.

METHOD-SELECTION STAGE

Before blind execution:

1. Commit an initial plan that declares:

   - the eligible proposal families;
   - development records;
   - search or comparison procedure;
   - compute ceiling;
   - selection rule;
   - failure handling.

2. Conduct any development comparison without using blind records.

3. Commit a blind configuration that freezes:

   - the designated primary challenger;
   - exact model/state hashes;
   - hyperparameters;
   - candidate ordering and tie-breaking;
   - candidate budgets;
   - target-update instances;
   - cuts;
   - record count;
   - statistical analysis;
   - stopping rules.

The designated primary challenger must be selected before blind observations or truth are opened.

Other challengers may be carried into the blind run as explicitly secondary comparisons if they were also frozen beforehand.

CANDIDATE BUDGETS

Evaluate at least four predeclared candidate budgets that:

- include budget 16;
- include at least one budget below 16;
- include at least two budgets above 16.

Default budgets:

- 8;
- 16;
- 32;
- 64.

Codex may replace this exact set before blind execution when a documented preflight or resource constraint supports a stronger design. Any replacement must retain budget 16 and at least four distinct budgets spanning lower and higher cost.

TARGET CONDITIONS

Evaluate:

1. one matched-public control; and
2. at least three independently pinned unavailable target-prefix updates.

The unavailable-update set should include:

- the accepted TRR-0001 target LoRA with SHA-256  
  `34d92f1e664236bfa1990b10148e8ad52c60b16e72ed0ff4c7eb7da8d15019f6`;
- at least two additional evaluator-only target updates generated without blind records.

The default additional updates should use the same early-prefix rank-4 LoRA construction with independently preregistered seeds and training-record assignments so that proposal robustness can be assessed without changing the target-update family.

Codex may use another justified construction when it yields a more informative robustness test, provided it is declared before blind evaluation and does not weaken comparability.

Use the same fresh blind records across target updates to permit paired comparisons.

FRESH DATA

Use at least:

- 64 fresh blind records;
- 2,496 scored tokens per target condition and cut.

The fresh blind records must be disjoint from:

- every TRR-0001 original split;
- every TRR-0001-R1 clean split;
- every excluded or failed attempt whose records differ from those splits;
- all TRR-0002 development, target-update-training, and proposal-training records.

SELECTION COMMITMENT AND RECORD PRIVACY

Reuse or strengthen the accepted R1 protocol:

- evaluator-private random selection key;
- cryptographically hiding public commitment;
- opaque record IDs independent of source content and row index;
- no pre-freeze disclosure of row indices, source hashes, text, token IDs, or a recomputable selection seed;
- post-freeze reveal and verification;
- truth read only after reveal verification.

Commit the public commitment before blind reconstruction.

The reconstructor-facing configuration and observation index must remain non-resolving and fail closed on prohibited metadata.

ACCESS ISOLATION

Reuse or strengthen the accepted namespace-and-`pivot_root` boundary.

Before each blind reconstruction process, verify that it cannot access:

- source mapping;
- dataset content cache;
- truth;
- evaluator-private target updates;
- callable target prefix;
- repository workspace;
- undeclared mounts;
- network routes.

Public resources or prepared public state genuinely required by a challenger may be exposed when:

- declared in the blind configuration;
- immutable and hashed;
- not sufficient to identify current source records;
- listed in the mount and runtime manifest.

Describe evidence precisely:

- claim dataset-content or cache absence when that is what was probed;
- claim package absence only when package absence was independently tested;
- enumerate the mounted runtime tree or an exact package allowlist.

Run each proposal-method and budget configuration under an auditable access boundary. Codex may safely share frozen proposal computation across budgets when the smaller candidate sets are deterministic prefixes of one predeclared ranking and the accounting remains exact.

PREREGISTERED PRIMARY COMPARISON

Primary condition:

- unavailable target updates;
- cut 4;
- candidate budget 16;
- designated primary challenger versus accepted residual-affine proposer;
- same fixed causal selector.

Primary outcome:

- end-to-end causal token accuracy excluding BOS and padding.

Primary mechanism outcome:

- frozen top-16 true-token recall.

Report:

- each target-update result separately;
- an equal-weight aggregate across the fixed target updates;
- paired record-level differences within each update;
- an update-stratified or nested record bootstrap;
- exact update and record counts.

The aggregate claim remains scoped to the fixed evaluated updates; do not treat three updates as a population-level model-family sample.

SECONDARY COMPARISONS

Report the complete predeclared frontier across:

- proposal method;
- candidate budget;
- cut;
- target update;
- matched-public control.

Do not select the most favourable blind budget after truth and present it as though it were the primary comparison.

If multiple challengers are carried into blind evaluation, either:

- predeclare one primary challenger and label the others secondary; or
- predeclare a multiplicity-aware decision rule.

METRICS

Report at minimum:

1. Frozen top-k true-token recall for every budget.
2. Exact full-vocabulary true-token rank after truth opening.
3. Direct proposal top-1 token accuracy.
4. End-to-end fixed-selector token accuracy.
5. Conditional selector accuracy given true-token inclusion.
6. Exact sequence-match rate.
7. Correct prefix length and first-error position.
8. Accuracy before and after first causal error.
9. Teacher-prefix counterfactual after truth opening.
10. Coverage and abstentions.
11. Proposal runtime.
12. Selector runtime.
13. Total runtime per record and scored token.
14. Peak CPU RSS by method and budget.
15. Peak GPU allocated and reserved memory by method and budget.
16. Embedding comparisons, candidate simulations, and model evaluations.
17. Proposal-training or preparation time and memory.
18. Trainable parameters and persisted-state bytes.
19. Implementation-complexity summary.
20. Failures, exclusions, and reasons.

STATISTICAL ANALYSIS

Use records as the primary resampling unit.

For each fixed target update, report:

- point estimates;
- paired differences;
- 95% confidence intervals;
- effect sizes;
- records and scored-token counts.

For the cross-update summary:

- weight each fixed update equally;
- preserve within-update pairing;
- clearly distinguish record uncertainty from target-update heterogeneity.

An end-to-end improvement claim requires a supported gain in causal token accuracy at the predeclared primary budget.

A proposal-recall claim may be made separately when its paired interval supports a gain.

A Pareto claim requires both quality and complete cost evidence.

If recall improves without an end-to-end gain, report that conversion failure rather than calling the reconstruction method improved.

TRUTH SEPARATION AND FREEZE

Before revealing mapping or truth, freeze:

- proposal states;
- candidate IDs and scores for every method and budget;
- direct outputs;
- causal outputs;
- query tensors;
- selector decisions;
- opaque record order;
- target-condition routing;
- method and budget configuration;
- access manifests;
- runtime and memory decisions;
- stopping and abstention state;
- all hashes needed to reproduce the comparison.

Create and verify a machine-readable receipt.

Then:

1. reveal and verify the hidden selection mapping;
2. verify disjointness and eligibility;
3. verify the receipt and access manifests again;
4. open truth;
5. score without revising any frozen output.

REQUIRED TESTS

Add or retain tests covering:

- opaque-record and prohibited-metadata validation;
- hidden commitment and reveal verification;
- disjoint fresh selection;
- truth denial before all gates;
- output immutability after freeze;
- access-boundary denial probes;
- exact state hashes;
- accepted budget-16 selector semantic compatibility;
- deterministic candidate ordering;
- candidate-set nesting when that optimisation is used;
- method and budget accounting;
- proposal recall and exact-rank metrics;
- paired and stratified bootstrap correctness;
- no target-prefix or correctness-feedback access;
- method-specific timing and memory completeness;
- failure on missing target-update or budget arms.

REQUIRED ARTIFACTS

Commit at minimum:

- `coordination/requests/TRR-0002.md`
- `coordination/results/TRR-0002.md`
- `experiments/TRR-0002/plan.json`
- `experiments/TRR-0002/method_selection.json`
- `experiments/TRR-0002/blind_configuration.json`
- `experiments/TRR-0002/selection_commitment.json`
- `experiments/TRR-0002/selection_reveal.json`
- `experiments/TRR-0002/selection_verification.json`
- `experiments/TRR-0002/target_updates.json`
- `experiments/TRR-0002/freeze_receipt.json`
- `experiments/TRR-0002/manifest.json`
- `experiments/TRR-0002/metrics.json`
- `experiments/TRR-0002/per_record_metrics.jsonl`
- `experiments/TRR-0002/diagnostics.json`
- `experiments/TRR-0002/frontier.csv`
- `experiments/TRR-0002/frontier.svg`
- `experiments/TRR-0002/final_validation.json`
- exact test outputs
- source and tests.

Large model states, candidate tensors, observations, and truth may remain outside Git when necessary. Record:

- path;
- role;
- immutable source or generation identity;
- byte count;
- SHA-256;
- retention status;
- exact regeneration command.

REQUIRED RESULT

coordination/results/TRR-0002.md must clearly distinguish:

- preregistered questions and decision rules;
- development method selection;
- frozen blind configuration;
- measured proposal recall;
- measured end-to-end reconstruction;
- target-update heterogeneity;
- budget frontier;
- quality-cost interpretation;
- post-truth diagnostics;
- failed and excluded attempts;
- deviations and their decision time;
- supported claims;
- unsupported claims;
- recommended next action.

CLAIM SCOPE

Claims must remain scoped to the evaluated:

- model and tokenizer;
- public dataset;
- proposal states and training resources;
- target updates;
- cuts;
- sequence geometry;
- candidate budgets;
- selector;
- blind records;
- hardware and implementation.

Do not generalise to all language models, target updates, cut depths, or distributed systems.

IMPLEMENTATION AUTONOMY

Codex may:

- propose stronger candidate methods;
- revise the development comparison;
- improve execution efficiency;
- strengthen isolation;
- choose a better target-update variation design;
- alter implementation details.

Material deviations must record:

- what changed;
- why;
- when;
- whether blind observations or truth had been opened;
- effect on comparison and claim scope.

The fixed compatibility requirements are:

- merge accepted TRR-0001 first;
- preserve the charter;
- retain the accepted residual-affine proposer as a baseline;
- retain fixed selector semantics;
- include budget 16 and a multi-budget frontier;
- include multiple unavailable target updates;
- use fresh hidden-selection blind records;
- freeze before mapping or truth reveal;
- report complete proposal and end-to-end cost.

STATE AND DELIVERY

After merging TRR-0001 and creating `task/TRR-0002`, update coordination/STATE.json to record:

- `protocol: TRR-RELAY/1.0`;
- `last_accepted_task: TRR-0001`;
- `active_task: TRR-0002`;
- `status: IN_PROGRESS`;
- `branch: task/TRR-0002`;
- request path;
- result path;
- manifest path;
- unchanged charter hash;
- updated UTC time.

At completion:

1. set status to `AWAITING_CHATGPT_REVIEW`;
2. commit and push `task/TRR-0002`;
3. open a pull request titled:

   `[TRR-0002] Improve candidate proposal and map quality-cost frontier`

4. identify the plan, method-selection record, blind configuration, selection commitment and reveal, target-update record, freeze receipt, metrics, frontier, result, and manifest in the pull-request description;
5. report the exact pull-request number and exact full final head after all metadata updates are pushed.

STOPPING CONDITION

Stop when:

- accepted pull request #2 is merged with its reviewed head preserved in main ancestry;
- development method selection is complete;
- the blind configuration is committed before blind execution;
- the fresh source mapping remains hidden until freeze;
- the accepted proposer and at least one challenger complete;
- the multi-budget frontier completes;
- matched-public and all required unavailable-target conditions complete;
- cuts 4 and 8 complete;
- candidate lists and reconstruction outputs are frozen before mapping or truth reveal;
- commitment, isolation, freeze, reveal, and scoring gates pass;
- proposal, end-to-end quality, runtime, memory, state, complexity, and uncertainty are reported;
- original and clean TRR-0001 evidence remain preserved;
- the next recommended action is identified but not started;
- the TRR-0002 pull request is open;
- coordination/STATE.json says `AWAITING_CHATGPT_REVIEW`;
- the exact final PR head is reported.

If no challenger improves the primary end-to-end result, report the null result and its uncertainty without retuning against blind truth.

END TRR CONTROL PACKET