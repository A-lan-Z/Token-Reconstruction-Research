New bounded phase alongside fit_gate_review's now-authorized14cell prediction run: prepare the existing transfer evaluator-side analysis entrypoint using scripts/trr0011_transfer.py and inherited design_v2. Reuse explicit baseline_wrong/broken/improved/unchanged inventory and evaluator_only_fixed_auc on baseline-correct risk set (rawH,relativeH,margin-normalizedfeature shift,paramL2;UNKNOWN no classes/nearzero spread), no thresholds/calibration. NewB1only7targets×2domains. Do not inspect any fresh truth/source/token files now; complete14cellprediction integrity/freeze is prerequisite. Agent2 owns private source/token custody and can execute your evaluator helper on its allowed truth after freeze, returning reportable diagnostics. First inspect actual existing helpers and coordinate expected prediction/geometry artifacts directly with fit_gate_review; implement only missing thin integration, not new framework. Prepare a concrete scoring handoff/commands or usable callback for Agent2 to run after freeze while its remaining A1GPUcells execute if CPUcost fits. Keep CPUthreads1 and boundedmemory, noGPU unlessneeded/preflightcoordinated. No new data,targets,fits,testmatrix or permissions. Root reviews scientific outputs and combines report. Report actual interface blocker early.
