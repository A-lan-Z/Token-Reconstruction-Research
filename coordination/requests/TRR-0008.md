# TRR-0008 — Does the frozen stronger positionwise decoder justify replacing the reference?

## Objective

Adjudicate the promising TRR-0007 candidate against the actual frozen
positionwise reference, with trustworthy timing and an independent
natural-text comparison.

Distinguish:
- improvement over the retained reference;
- improvement over another credible residual decoder;
- rescue of a weaker same-bank control.

This is a bounded frozen-model study. No new training, checkpoint
selection, architecture sweep, teacher objective, or compression.

## Starting point and ownership

Repository: A-lan-Z/Token-Reconstruction-Research

Parent branch: task/TRR-0007
Reviewed head: 91925feb86dc03b912324b342a10282df27d79cd
New task/branch: TRR-0008 / task/TRR-0008

Verify current task-name and branch availability before creating anything.
Use a separate worktree, task-owned artifacts, and task-local state.
Read the charter and relevant TRR-0007 source/evidence.

Do not merge PRs, change global coordination or active benchmark state,
or modify the parallel agent's workspace.

Agent A2's TRR-P06 owns the later-position activation experiment.
Do not duplicate it or use unpublished results. Continue approved
hash-only source/sequence exclusion coordination. P03's sealed holdout
remains out of scope.

## Frozen methods

Read exact selected-state hashes and execution identities from
TRR-0007/method_freeze.json.

Retain:
- the frozen TRR-0006 positionwise reference;
- current_enriched__residual_mlp512;
- improved_public_bank__residual_mlp512;
- improved_public_bank__trained_diagonal as a secondary diagnostic.

The primary candidate is improved_public_bank__residual_mlp512.

The current_enriched__trained_diagonal predictions were identical to
the reference in TRR-0007. Do not treat them as independent scientific
comparators. Their alias may be useful for timing validation.

All methods remain current-activation-only, full-vocabulary, and
search-free. No A2 fallback or token-history inputs.

## 1. Resolve the timing ambiguity before consuming new evaluation data

Use already-opened observations and the frozen models.

TRR-0007 measured approximately 4.04 versus 5.89 ms for a same-weight
reference/current comparison on Pile/public. The stronger candidate's
reported runtime penalty therefore cannot yet be attributed cleanly
to model capacity.

Use repeated blocked timing with balanced or randomized method order,
equivalent workloads, explicit synchronization, and comparable warmup,
batching, loading, and validation boundaries.

Include an identical-model/identical-weight control through the relevant
execution paths. Establish prediction equivalence for any harness change;
do not assume that changed batching or precision is harmless.

Report warmed latency or throughput with its actual definition, variability,
memory, and startup separately. Do not select the fastest observed ordering
or average away a persistent cost failure.

Preserve the existing 1.25x warmed-runtime qualification threshold for this
follow-up. If a valid measurement establishes that the candidate exceeds
the budget, report that before committing to a large fresh panel.
Do not optimize the architecture until it passes.

## 2. Plan one decision-focused independent comparison

Make candidate-versus-frozen-reference the central contrast. Retain the
current-bank residual as a credible alternative, and the new-bank
trained-diagonal arm to diagnose weaker-control rescue.

Finance exact-clip recovery is the motivated primary endpoint.
Predeclare how the paired shifted target and Pile safeguard contribute
to the decision. Avoid recreating the previous 64-direction factorial
primary family when only a few deployment decisions are now at issue.

Preserve the existing distinction between:
- a reliably positive gain;
- a practical gain of +5 percentage points in exact recovery or
  +1 percentage point in token accuracy;
- compliance with the relevant token/exact harm and cost limits.

Do not weaken practical thresholds after seeing outcomes. A narrower
primary hypothesis is allowed prospectively; it is not a revision of
TRR-0007's results.

Choose a fixed sample size from paired-discordance planning and source
availability, including sensitivity to smaller true effects.

The pilot candidate/reference exact counts are 13 gains and 6 losses
out of 128 matched Finance sources. Their net +5.47-point estimate is
only slightly above the +5-point threshold. A moderate panel may establish
a positive gain without establishing a gain greater than that threshold.
Do not promise otherwise or plan an unbounded sample expansion.

Check the remaining eligible source inventory before committing to a
record count. If a source range must be extended, freeze the extension
and its interpretation before selection. Do not reuse opened sources
and call them fresh.

## 3. Run the frozen comparison

Use new natural source records with the established rendering, 128-token
clip including BOS, and all 127 scored post-BOS positions.

Pair identical sources across the declared public-base and synthetic-LoRA
targets. Preserve qualified observation generation and numerical semantics.
Do not count target conditions as independent sources.

Freeze all choices and predictions before scoring. Use bounded chunks
and a complete pre-score validation. No partial-score-driven stopping,
model swaps, or execution changes.

Report domains and targets separately:
- token errors and accuracy;
- exact-clip counts and rates;
- paired gains/losses and record-level uncertainty;
- candidate versus reference, current residual, and same-bank control;
- counterbalanced inference cost and deployed footprint.

A small preregistered A1+A2 subset is optional only if it answers a concrete
quality question. Do not rerun A1+A2 over the entire panel by default.
State any anchor denominator separately.

This confirms particular frozen checkpoints. It does not establish that
every model trained with this architecture or recipe will behave the same.

## Decision and stopping

Advance only when the prospective quality and cost criteria are met.
A domain-specific benefit must remain labelled domain-specific.

If the candidate improves only over the weaker same-bank control,
does not improve over the retained reference, or fails the cost rule,
do not promote it.

If it reliably improves but its practical magnitude remains uncertain,
report that distinction. Do not automatically retrain or double the sample.

If the confidence bounds remain inconclusive under the bounded design,
retain the reference and state the unresolved decision rather than
calling the candidate ineffective.

Whatever the outcome, keep the much larger A1+A2 quality gap separate.
This task chooses a better standalone checkpoint; it does not solve
reconstruction by definition.

## Deliverables

No paid compute. Coordinate shared resources and keep comparative timing
uncontended.

Save this assignment at coordination/requests/TRR-0008.md.
Write results to coordination/results/TRR-0008.md and structured evidence
to experiments/TRR-0008/manifest.json, with task-local status.

Commit reproducible commands, planning assumptions, state/input identities,
frozen predictions, timing blocks, costs, exclusions, and failed attempts.
Open a follow-on PR against the actual parent without merging.

Lead with the decision: genuine improvement over the reference, weaker-
control rescue, cost failure, or unresolved practical benefit.