BEGIN TRR CONTROL PACKET
PROTOCOL: TRR-RELAY/1.0
PACKET_TYPE: BOOTSTRAP_AND_VALIDATION
PACKET_ID: TRR-PACKET-BOOTSTRAP-0001
REPOSITORY: CURRENT_WORKING_REPOSITORY

EXPECTED_STARTING_MAIN_COMMIT:
50cce6453644cac70fee629d6566ddc9504a0d71

PREVIOUS_TASK_ID: NONE
PREVIOUS_DISPOSITION: NONE

NEXT_TASK
TASK_ID: TRR-0000
TITLE: Bootstrap and validate the repository-backed research relay

PURPOSE

This is an orchestration and reproducibility validation task. It is not a
token-reconstruction experiment and must not select or restrict a scientific
research direction.

The task should establish the committed repository state required for ChatGPT
Pro to review future work directly through GitHub and should demonstrate that a
Codex task can produce a complete, reproducible, reviewable pull request.

INPUT CONDITIONS

The working tree may already contain uncommitted scaffold files created by the
user. Inspect and preserve them. Do not reset, delete, or overwrite useful
uncommitted work merely to recreate it.

RESEARCH_CHARTER.md must contain the user's supplied research goal and access
model verbatim. Do not paraphrase, reinterpret, expand, or add scientific
conditions to it.

If RESEARCH_GOAL_AND_ACCESS_MODEL.md is byte-for-byte identical to the intended
RESEARCH_CHARTER.md, preserve history through a Git rename. If it differs, do
not treat it as the authoritative charter. Preserve the difference clearly and
ensure RESEARCH_CHARTER.md is the sole designated authority.

REQUIRED WORK

1. Inspect:
   - the current branch;
   - the working tree;
   - the configured Git remote;
   - the current local and remote main commits;
   - all existing scaffold files;
   - AGENTS.md.

2. Record any difference from the expected starting commit as a material
   repository-state observation. Do not discard work to force the expected
   commit.

3. Create and work on:

   task/TRR-0000

4. Save this complete packet verbatim as:

   coordination/requests/TRR-0000.md

5. Ensure the following committed paths exist:

   RESEARCH_CHARTER.md
   AGENTS.md
   coordination/STATE.json
   coordination/requests/TRR-0000.md
   coordination/results/TRR-0000.md
   research/ROADMAP.md
   research/DECISIONS.md
   experiments/TRR-0000/manifest.json
   scripts/trr0000_smoke.py

6. Keep research/ROADMAP.md and research/DECISIONS.md neutral at this stage.
   They may define their purpose and initial empty state, but must not invent
   research findings, permanent methodological rules, or scientific decisions.

7. Implement a dependency-light deterministic smoke script at:

   scripts/trr0000_smoke.py

   The script should:
   - accept an explicit seed and output path;
   - create a small deterministic JSON result;
   - include a schema or format version;
   - avoid machine-learning dependencies;
   - exit nonzero on invalid arguments or write failure.

8. Run the smoke script at least twice with the same declared inputs and verify
   that the scientific/data payload is identical. Runtime timestamps and timing
   measurements may be recorded separately and are not expected to be
   deterministic.

9. Add an appropriate automated check or test for the deterministic result and
   run it.

10. Compute and record:
    - the SHA-256 of RESEARCH_CHARTER.md;
    - the SHA-256 of the deterministic smoke output;
    - the exact commands executed;
    - Python and relevant tool versions;
    - operating-system information;
    - start and end timestamps;
    - exit statuses;
    - the repository base commit;
    - generated artifact paths.

11. Write the structured evidence to:

    experiments/TRR-0000/manifest.json

12. Write the human-readable handoff to:

    coordination/results/TRR-0000.md

    It must include:
    - status;
    - base repository state;
    - files created or changed;
    - exact commands executed;
    - test results;
    - deterministic-output verification;
    - artifact hashes;
    - deviations or blockers;
    - branch and pull-request information.

13. Update coordination/STATE.json with at least:
    - protocol;
    - charter_sha256;
    - last_accepted_task;
    - active_task;
    - status;
    - branch;
    - pull_request;
    - request_path;
    - result_path;
    - manifest_path;
    - updated_utc.

    Do not place a self-referential current head commit SHA in the committed
    STATE.json. The pull-request head must be obtained from GitHub.

14. Validate all JSON files by parsing them.

15. Commit the work, push task/TRR-0000, and create a pull request titled:

    [TRR-0000] Bootstrap and validate research relay

16. After the pull request exists, update its description with:
    - the request path;
    - result path;
    - manifest path;
    - commands used to validate the work.

17. When necessary, update coordination/STATE.json with the pull-request number,
    commit that update, and push it to the same branch.

18. Report the exact final pull-request number and exact final PR head commit in
    the Codex response. Obtain the final head SHA from Git after all task files
    and metadata updates have been pushed.

IMPLEMENTATION AUTONOMY

Choose reasonable implementation details for the smoke test and repository
scaffold. Do not introduce a scientific method, experimental direction, or
additional restriction on the research charter.

STOPPING CONDITION

Stop when all required files are committed, the checks pass, the task branch is
pushed, the pull request is open, coordination/STATE.json says
AWAITING_CHATGPT_REVIEW, and the exact final PR number and head commit have been
reported.

If GitHub authentication genuinely prevents pushing or opening the pull
request, preserve all completed local work and report the exact failed command
and error as the blocker.

END TRR CONTROL PACKET