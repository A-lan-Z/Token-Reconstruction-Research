# TRR-0012 closeout control packet

Bounded closeout evidence packaging task in TRR-0012. Inspect only task-owned outputs to identify small reproducibility artifacts still untracked for actual transfer successful14cell run, its launch/input/matrix/freeze/cell metadata/prediction files, and required failed-attempt receipts. Prepare an explicit staging allowlist at experiments/TRR-0012/report/final_evidence_allowlist.json with sizes and role, excluding geometry tensors (~1.4GB), model states/E, banks and large records. Check any crucial referenced small metadata needed to reproduce successful native pair is missing from git, include if directly necessary; avoid broad audit or duplicate checks. Do not stage/commit or alter main report/status (starting_recipe owns those). No access to Agent2 private files. No new tests/GPU. Also condense the overhead lesson into 5-8 sentences for report: environment/path/shape integration and too many serial handoffs cost time; future qualify cold full entrypoint + scorer on synthetic shape end-to-end before reserved compute, use one owner per complete phase, release GPU during repair, preserve hash/truth gates. This is operational improvement, not new experiments.
