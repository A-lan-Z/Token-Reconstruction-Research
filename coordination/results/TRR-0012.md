# TRR-0012 — New-weight fixed-readout replication (in progress)

The exact common starting decoder and the original 1,200-record B0 activation bank survived and match their published file hashes. The public model, tokenizer, fixed normalized readout, raw public source caches, and ordered 12,000-record B1 identity ledger also survive. The expanded-bank input tensor and ordered record files have now been reproduced byte for byte. This required explicitly pinning the original 7 September 2026 date inserted by the Alpaca chat template. Earlier current-date attempts are retained and excluded. Public validation sidecars and B0’s semantic sampling schedule are also recovered; activation capture and B1 schedule preparation remain pending.

No new fit or independent evaluation has completed. This is a new-weight replication; the original TRR-0011 frozen-checkpoint confirmation remains blocked/incomplete from missing selected artifacts, not a scientific failure. Its loss evidence and the PR20 selection-overlap correction remain unchanged.

The shared design is recorded in `experiments/TRR-0012/shared_contract_v1.json`. The exact original budget is 13,000 updates per bank, seed 4010, eight records and 512 sampled positions per update. Selection considers only steps 0, 1,000, 2,000, 4,000, 8,000, 12,000 and 13,000, using the earliest strict maximum of equal-domain public development token accuracy. The historical validation-every-1,000 metadata did not create additional eligible checkpoints.

Historical incremental fit wall times were 574.74 seconds for B0 and 1,762.88 seconds for B1, excluding public preparation and inherited training. Each fit peaked near 2.76 GiB reserved GPU memory and 3.40 GiB host RSS. These are planning references, not measurements of the new replication.

Agent 1 owns one new paired fit and its durable package. Agent 2/TRR-P11 owns exclusions and one common evaluation: 256 unused sources per domain, paired across public-base and historical public-LoRA targets, with the separately reported A1+A2 subset fixed to the first 128 sources per domain. Domains and target cells remain separate. Evaluation cannot begin until all required exclusions, selected-state freezes, and the mandatory independent restore pass.

The restore fixture is frozen before fitting: the first two already-opened TRR-0010 public-base Finance records, then the first two Pile records. Expected outputs will be generated only after ordinary checkpoint selection. Actual selected weights and deployment resources must exist in persistent WSL storage and a verified Windows copy. Agent 2 must retrieve the Windows copy into a clean persistent directory, verify file/tensor identities, and reproduce all eight method-record outputs. The Windows copy protects against WSL reset loss, not whole-host failure.

Structured evidence: `experiments/TRR-0012/manifest.json`. Current task-local execution state: `experiments/TRR-0012/status.json`. No PR merge, global coordination/registry change, paid compute, or P03 sealed-holdout access is authorized.

The next scientific decision is whether the rebuilt expanded-bank advantage generalizes to unused sources. No result or change-of-direction conclusion is available yet.
