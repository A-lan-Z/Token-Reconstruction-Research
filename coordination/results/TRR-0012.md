# TRR-0012 — New-weight fixed-readout replication

## Current result

The native TRR-P09 fixed-control runner completed both new-weight fits from the frozen common contract. B0 selected step 8,000 and B1 selected step 13,000 under the exact earliest-strict-maximum rule on the seven-point public-validation grid. Independent metadata-only audits recomputed the equal-domain metric from recorded Finance/Pile counts and confirmed each selected-state binding. These accuracies are public development labels; they are not independent evaluation claims.

No independent evaluation, durable package restore, or package output smoke comparison has completed. Selected states and the additive timing package remain pending the actual package/restore handoff.

## What survived and what was rebuilt

The common starting decoder, original P09 loader/source contract, public model/tokenizer/readout and caches, and the 1,200-record B0 prefix survived with their existing bindings. B1 additions were rebuilt from the historical 07 September 2026 Alpaca rendering date and consumed from `experiments/TRR-0012/preparation/b1_cpu_r4_date07`; input SHA-256 is `8150184a349bebd31e24b128d23f096702898024a87a405322ee3ba95fa6682e` and records SHA-256 is `367cfba0ffe78f59454861a76f23830f480a8745cc6b35e6b4a0d05eca53638b`. Earlier current-date/source26e preparations are retained and excluded. The exact original source target was commit `26e08098135c47a5607aff915e8025b57a747e20`; the fits actually ran from read-only worktree content at `cab7d455c305aa01c3d3bfac225fcffa3e66640b`, with training wrapper commit `c943663`. Both fits used seed 4010, 13,000 updates, and 512 position draws per update.

## Native fit and selection evidence

| bank | records | selected step | development score | selected state bytes | external watchdog wall |
|---|---:|---:|---:|---:|---:|
| B0 | 1,200 | 8,000 | 97.1195% | 29,390,260 | 585.260011 s |
| B1 | 12,000 | 13,000 | 99.4433% | 29,390,268 | 2,445.085954 s |

Each bank used 6,656,000 total position draws. Full selected-state file and logical SHA-256 identities are recorded in `manifest.json` under `fit.banks.B0.selected_state` and `fit.banks.B1.selected_state`. The audits did not read state bytes, load E/model/tensors, use torch/GPU, or launch experiments. B0 audit SHA-256 is `0c6cf536cb00ebc6e43db31aabed070203f75e217b26f0ed13d0063f01e42065` and B1 audit SHA-256 is `df4e21bb7174d5bc227c9e9ff26a289bb59612e084c9901e2ce181089b9f3ed2`; native receipt SHA-256 values are `40c4d42319c786ed7b65dd9e13aaa2b5a7e952b3d96e5a579f4b5bc095188e09` and `de6ee6380d32318d574e0575332906cbab721a237a78fe4165f2f6e4015d11b2` respectively.

The external watchdog walls sum to **3030.345965 seconds (50.505766 minutes, about 50.5 minutes)**. Nested native phase timings are separate:

| bank | optimizer update | stream load | validation | inner whole process |
|---|---:|---:|---:|---:|
| B0 | 482.525467 s | 44.982888 s | 13.487779 s | 567.011871 s |
| B1 | 485.602663 s | 1,729.112992 s | 13.325601 s | 2,432.327989 s |

Do not add the inner phases to the watchdog walls. The expanded fit's additional measured inner cost is overwhelmingly stream loading (B1 minus B0: 1,684.130104 s); optimizer update differs by 3.077196 s. Loader caching would require output and schedule equivalence and was not introduced or rerun. Native in-process peaks were 2,533,930,496 GPU allocated bytes, 2,959,081,472 GPU reserved bytes, and 3,648,749,568/3,651,186,688 process RSS bytes for B0/B1.

## Preparation cost and deviations

The separate ledger `experiments/TRR-0012/report/preparation_costs.json` (SHA-256 `2d333e016a43e2cbc126c1be85febd27a0458bcf7d54690abed96abc064be1ac`) reports **250.496860 seconds** across distinct measured external invocations. Added to the two fit watchdog walls, this gives **3280.842825 seconds (54.680714 minutes)** of partial recorded external compute. That labeled total excludes unknown/historical work and does not add nested inner phases a second time. The ledger covers the completed/excluded public-input attempts, public-validation and schedule preparation, capture qualification and full capture, native configuration dry-runs, and fixed native qualification attempts. Source snapshot creation, starting-decoder ancestor preparation, and historical public-normalized-embedding preparation have no TRR-0012 timing receipts and remain unknown/historical.

The ledger preserves the material exclusions: current-date B1 r2/source26e attempts were superseded by date-pinned r4; initial capture qualification stopped on import/source-root metadata failures before model use and r2 passed; configuration dry-run r1 closed on the immutable-B0-binding check and r2 passed; fixed qualifier v1 preserved source-binding and watchdog-package-root failures before child/model execution; fixed qualifier v2 passed a bounded first-eight-update native probe with all updates discarded and no retained checkpoint or contender. Exact evidence paths and hashes remain in the ledger.

The workflow lesson is to persist selected deployment bytes, a secondary copy, and the canonical exclusion ledger at fit completion; reconstructing those pieces later added avoidable preparation overhead. This observation does not change the completed replication.

## Pending gates and historical limits

- Assemble the actual selected-state package, complete the persistent Windows copy, and perform clean restore/tensor-identity checks.
- Compare ordinary package predictions against the byte-preserved original producer, including the deferred four-record smoke; the bounded package test below did not run inference.
- Run the pending independent evaluation over 512 independent sources total (256 per domain) across four target cells, with 256 source rows per cell; separately report the A1+A2 subset fixed to the first 128 sources per domain. No independent score exists and evaluation truth remains unopened.
- TRR-0011 frozen-checkpoint confirmation remains incomplete because selected artifacts are missing; this is an incomplete confirmation, not a scientific failure. The PR20 selection-overlap correction remains preserved.

## Focused package timing test

`experiments/TRR-0012/report/package_timing_test_receipt.json` (SHA-256 `6c745e1c5ed6ef3d3a2a85a815d7e31e364a53d601a715e8716d7ef19036ee96`) records the bounded CPU test: **9 passed, 1 skipped**, external wall **1.99 s**, pytest-reported wall **1.24 s**, and maximum RSS **676,848 kB (692,068,352 bytes)**. The skip is the explicitly gated post-fit package smoke; no E/state/model inference was run. The wrapper did not emit UTC start/end timestamps, so the receipt records them as unmeasured. Exact stdout is preserved in the receipt.

The structured integration is `experiments/TRR-0012/manifest.json`; the prior preparation status/stage are preserved under `historical_preparation_status`, while the current status reflects completed fits with package, restore, and evaluation pending.
