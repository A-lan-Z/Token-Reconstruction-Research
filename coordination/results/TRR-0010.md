# TRR-0010 — Stage-1 public bank capture complete; fits pending

The signed template-compatible plan and its correction history remain bound in
`experiments/TRR-0010/planning/`. The guarded compatible-parent CPU rerun
completed 12,000 records, 1,243,710 valid post-BOS positions, support size
45,631, and the formula-derived common schedule `N=13000`. The clean
qualification receipt, capture receipt, and A2 common-schedule receipt are
copied byte-exact under `experiments/TRR-0010/setup/` with their source paths
and hashes recorded in the task manifest.

Stage-1 public capture completed 10,800 additions in the half-open global
range `[1200,12000)` across 169 shards. The wrapper ran for 62.141337 seconds
(child and wrapper exit 0); the capture receipt's inner execution was
55.403302 seconds. Reserved GPU memory was 2,474,639,360 bytes, inner RSS
1,803,186,176 bytes, external wrapper RSS 1,842,196,480 bytes, and retained
bytes 8,545,446,344. Capture loaded public fitting inputs only; no source text
was written and no final truth was opened.

The capture-declared additions manifest (`f1ad02c2…`) is preserved as a
metadata reference but remains revision-pending and is not treated as final
B1. The read-only corrected-r2 audit records the quota, support, N, and zero
overlap metadata; the authoritative final B1 metadata/payload binding is still
required. Combined-loader metadata, B1 support metadata, and the inherited
384-record public-base validation metadata are bound: validation is Finance
256 plus Pile 128, public-base only, with 127 scored post-BOS positions per
record.

The earlier selector special-ID mismatch is preserved as a pre-fit excluded
failure and is resolved for this stage-1 capture. Directional merged-W
qualification, substantive four-arm fits, final panel selection/evaluation,
truth access, and every reconstruction-quality result remain pending. The
opened-fixture zero-Delta PASS and stage-1 qualification are resource/integrity
evidence only.

## Historical r1 checkpoint (retained)

# TRR-0010 — Preparation checkpoint: corrected r2 compilation pending

The r1 CPU preparation remains excluded before capture. It compiled 12,000
records and 1,243,710 post-BOS positions in 32.387874 seconds, but its
controlled joint assignment did not follow the B0-template amendment. The A2
follow-up audit also confirms that 63 of the 624 prior-development Alpaca keys
actually occur in r1 additions. No r1 capture, GPU qualification, substantive
fit, fresh evaluation, truth access, or score exists.

Root and A2 have jointly approved the exact correction addendum from commit
`537ba5c423ed480f6a3046fdff32a4e3b49deef3` (6364 bytes, SHA-256
`0bad461d93041fdb21d284b0ae1684d2268adfb0f9a304f9b97ab4cd39900564`). The
byte-exact task copy and CPU-r2-only amendment receipt are bound in the status
and manifest. The approval uses root thread
`01a07029-b0e2-71d2-af08-ecd9aacc645f` and A2 thread
`01a07061-9c25-7e13-bcac-12d57c41c666`; it authorizes implementation and
guarded CPU r2 compilation/verification only.

The current blocker is corrected compilation and verification: bind the
namespace-correct exclusion manifest, verify zero overlap for all 624 prior
Alpaca development keys, and retain a fresh guarded CPU receipt. Capture
qualification and any substantive fits remain downstream gates.


The first bounded opened-fixture equivalence attempt on 2026-09-07 exited 1 after 1.0 seconds before model, E, H, or observation payload load. It was excluded for a support-digest namespace mismatch; the failure receipt is `experiments/TRR-0010/evaluation/opened_fixture_zero_delta_v1_failure/failure.json`. A CPU-only provenance check then derived B0 support from public `token_ids` and `attention_mask` only: 124,371 post-BOS rows, 17,126 supported IDs, exact equality with the stored support vectors, and published TRR-0009 digest `8090b923…6969d` (TRR-0010's distinct digest is recorded separately). No truth, accuracy, fitting, or capture was accessed; retry requires a fresh lease.


The corrected opened-fixture diagnostic v2 passed on the exclusive RTX 5080 lease (child exit 0; 6.325812 seconds). Across the first two already-opened public-base records in each Pile and Finance domain, zero-Delta merged-E, base-only state reload, and effective-readout reload were all exact with maximum absolute difference 0.0. Peak CUDA reserved memory was 2,871,001,088 bytes; peak RSS was 3,679,838,208 bytes; minimum host available memory was 18,749,104,128 bytes. The 1,050,674,192-byte effective-readout export and 29,390,316-byte base-only export are bound by hash but remain outside Git. No fit, truth, label, or accuracy operation occurred.

## Current publication and compatibility state

Draft PR [#20](https://github.com/A-lan-Z/Token-Reconstruction-Research/pull/20)
is open and unmerged against `task/TRR-0009`; the exact submitted body and
receipt remain bound under `experiments/TRR-0010/planning/`. Stage-1 public
capture and common schedules are now receipt-bound, while the captured
addition metadata still needs its authoritative revision before B1 is treated
as final.

The clean stage-1 qualification passed and the corrected capture completed
10,800 new records in global range `[1200,12000)`, with 169 shards and no
truth access. The capture-declared `bank_manifest.json` hash is preserved as
metadata evidence only; it is not promoted to the final B1 manifest. The
combined loader, B1 support metadata, and the inherited 384-record public-base
validation metadata are bound without copying payloads. Directional merged-W
qualification, substantive four-arm fits, final evaluation, truth, and all
quality results remain pending.
