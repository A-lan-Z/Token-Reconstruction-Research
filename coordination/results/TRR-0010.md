# TRR-0010 — Current directional training complete; deployment export pending

Current execution remains truth-free and bounded at the fit stage. B0 current-fixed and B1 expanded-fixed completed all 13,000 updates cleanly. The current directional arm also completed its 13,000-update, 6,656,000-draw training run, but its post-fit selected-state/readout export adapter failed before deployment artifacts and fitting callbacks were retained; the raw runner receipt is preserved and the recovery is pending. The expanded directional arm has not launched. No final four-arm result, fresh reconstruction-quality metric, truth payload, or promotion claim is available.

The current directional raw runner selected step 0 by the predeclared public development validation metric. Across that public validation, domain-balanced token accuracy fell from 0.9681809793 at step 0 to 0.9589843750 at step 13,000 while cross-entropy fell from 0.1989119456 to 0.1771696456. This objective/greedy-accuracy divergence is a fit diagnostic only; it is not fresh natural quality evidence. The raw receipt is `experiments/TRR-0010/training/directional_fit/current_directional_watchdog_r2/fit/raw_runner_result.json` (62,160 bytes, SHA-256 `de89e05c2d93a7bd44b84fac8d70dea8a064722557e37869ea434c9f9f2019c7`). The provisional combined table is [`combined_fit_diagnostics_cost_table_v2.json`](../../experiments/TRR-0010/report/combined_fit_diagnostics_cost_table_v2.json), marked `TRAINING_COMPLETE_DEPLOYMENT_EXPORT_PENDING`; it contains no final evaluation or truth result.

The route-specific public-cost adapter is now implemented and synthetic-tested. It consumes the runner's post-warm `measured_seconds_sum` and per-cell reset `cuda_peak_reserved_bytes`, excludes shared model preparation from warmed runtime, and records host RSS as a whole-process high-water value rather than an isolated deployment gate. It validates the frozen v4 limits of 1.5x matched-fixed runtime and CUDA peak, plus 0.25x candidate-to-A1+A2 warmed runtime. Directional (expanded versus fixed/current-fixed/A1+A2) and expanded-fixed data routes remain separate. No production TRR-0010 timing artifact is bound yet, so the cost gate for the eventual score remains pending until the public prediction run completes.

## Fixed-arm fit diagnostics and cost (descriptive)

Both fixed-readout arms completed the full 13,000-update, 6,656,000-draw effort. B0 is the current-fixed bank and selected step 8,000 by the predeclared earliest strict maximum of opened public development validation; B1 is the expanded-fixed bank and selected step 13,000. The complete 14-row checkpoint table, including frozen-64 and endpoint full-bank diagnostics, is bound at [`fixed_fit_learning_curve_cost_table_v2.json`](../../experiments/TRR-0010/report/fixed_fit_learning_curve_cost_table_v2.json). The v1 table remains byte-preserved as historical metadata; v2 replaces only its provisional unknown B1 exposure counts with the published A2 audit. These curves are fit and checkpoint-selection diagnostics, not final reconstruction quality.

| bank / role | available post-BOS positions | sampled unique pairs / repeat draws | support IDs | process wall (s) | diagnostic total (s) | peak CUDA reserved / host RSS (GiB) |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| B0 current-fixed | 124,371 | 124,371 / 6,531,629 | 17,126 | 574.739 | 11.297 | 2.756 / 3.402 |
| B1 expanded-fixed | 1,243,710 | 1,237,403 / 5,418,597 | 45,631 | 1,762.882 | 154.616 | 2.756 / 3.402 |

The B0 audit serialized the sampled pair support and repeats. The published A2 B1 audit now binds 1,237,403 sampled unique record-position pairs, 5,418,597 repeated position draws, 11,996 sampled records, and 104,000 record-batch exposures out of the 12,000-record composed bank. The B1 frozen-64 diagnostics use 19 position chunks per checkpoint; the support binding's 1,119,339 figure remains additions-only. Phase times are nested process accounting: optimizer update, stream load, and validation are not added to process wall, and diagnostic timings are reported separately. Cold disk startup and inherited preparation costs were not separately measured. The artifact preserves these boundaries and keeps the later directional-arm fields available without implying that those arms have run.

A metadata-only arithmetic correction is recorded in the current draft, status, and manifest: B0 contributes 124,371 valid post-BOS positions, B1 additions contribute 1,119,339, and the combined total is 1,243,710. The published A2 final manifest at source commit `02fa090140cc3c55172225122eb4d7649e995951` (79,938 bytes, SHA-256 `6371426c2ac587c90bdb86277b71c8a1274ebed625ab92a47e10d6b34f64c402`) supplies the B1 audit counts used in table v2. This supersedes only the descriptive B1/combined count fields; the qualification-only contract, schedule identities, payload bindings, and historical r1 metadata are preserved unchanged.

The signed template-compatible plan and correction history remain bound in
`experiments/TRR-0010/planning/`. The guarded CPU preparation and common
schedule are complete at 12,000 records, 1,243,710 valid post-BOS positions,
support size 45,631, and common `N=13000`. The clean qualification and capture
receipts remain byte-exact under `experiments/TRR-0010/setup/`. The B0 audit and
handoff receipts are bound at `/tmp/trr-p09-runtime/fixed-control-b0-r3-audit-r3/`
(SHA-256 `1f09e487…c6c6` and `9dc7c12…6851d`); they report the completed
13,000-update run, 6,656,000 draws, 124,371 unique record-position pairs,
and no truth or final-quality access. The B1 expanded-fixed completion receipt is
`/tmp/trr-p09-runtime/fixed-control-b1-r3/receipt.json` (7,111,987 bytes,
SHA-256 `bc75d1c6…3eed47a`), with selected step-13,000 state
`checkpoint_step_013000.safetensors` (29,390,268 bytes, SHA-256
`57575060…3ec3a`). Current directional training has completed, with deployment export and retained fit-diagnostic callbacks pending; expanded directional training has not launched.

The approved 64-record-per-bank diagnostic descriptor is also bound in that
draft and locally at
`experiments/TRR-0010/setup/fixed_diagnostic_binding_r1.json` (130,098 bytes,
SHA-256 `8b899bc1…adb1f54c3`). Its quotas are Alpaca 32, Pile natural 16,
Finance natural 10, Pile controlled 3, and Finance controlled 3; fixed and
directional arms share indices within each bank, and diagnostic rows are joined
by `global_row`, not by descriptive rank order. It is diagnostic only.

A stage-3 execution-contract draft is now bound at
`experiments/TRR-0010/planning/stage3_execution_contract_draft.json`. It
references the immutable v4 proposal, signed stage-1 plan, qualified B1
additions/support/schedule, and audited 384-record public-base validation. It
leaves only measured future final-evaluation qualification/caps, exact imported
runtime bindings, post-fit state/readout hashes, and final-panel/truth
registration pending; it introduces no new scientific choices.

A2's fixed-control CPU configuration receipts are also bound in the
stage-3 draft: B0 is 105,298 bytes, SHA-256
`bbfe2bf4…0270ffc78`, and B1 is 108,418 bytes, SHA-256
`6998adb7…37505f9`. Both use source commit
`8398666cb8d0222e613c362eae33d77d972ab385`, the shared 384-record public-base
validation descriptor, and the approved 64-record diagnostic binding. These
are configuration-only PASS receipts: public validation labels were loaded,
but no model, embedding, optimizer, fit, or final truth was opened.

The qualified B1 metadata manifest is now bound at external path
`/tmp/trr-p09-runtime/stage1-capture-r2/bank_manifest-qualified-r1.json`,
145,390 bytes, SHA-256 `0e14dbf7…7bb36d196`; its compact binding receipt is
committed locally. It covers the unchanged 10,800 additions in global range
`[1200,12000)`. The earlier `f1ad…` manifest remains preserved as historical
metadata only. The published A2 task manifest and B1 audit are separately bound
by source commit and hash above; raw shards and support payloads remain local
and are not copied into this task.

The combined capture descriptor and B0/B1 public-support descriptor are bound
without final-truth access. Stage-1 public capture qualification passed for the
B8x192 geometry. The separate forward-batch/resource qualification for the
future final-evaluation path remains a capacity gate. A2's 384-record
public-base validation manifest and rows now have a compact audit receipt
proving all record IDs, token IDs, masks, positions, and H file hashes exact;
this is checkpoint-selection evidence only and opened no model, LoRA,
activation, or final truth. Both B0 current-fixed and B1 expanded-fixed fits are now independently
audited; current directional training is complete but deployment export/recovery is pending, expanded directional training has not launched, and final evaluation and every reconstruction-quality result remain pending.

## Historical r1 checkpoint (retained; superseded by the current status above)

The chronology below records earlier preparation and excluded attempts. It does not override the current B0/B1 fixed-readout-complete status, the held directional fits, and the absence of final quality results.

# TRR-0010 — Preparation checkpoint: corrected r2 compilation pending

The r1 CPU preparation remains excluded before capture. It compiled 12,000
records and 1,243,710 post-BOS positions in 32.387874 seconds, but its
controlled joint assignment did not follow the B0-template amendment. The A2
follow-up audit also confirms that 63 of the 624 prior-development Alpaca keys
actually occur in r1 additions. No r1 capture, GPU qualification, substantive
fit, fresh evaluation, truth access, or score exists.

Root and A2 have jointly approved the exact correction addendum from commit
`537ba5c423ed480f6a3046fdff32a4e3b49deef3` (6364 bytes, SHA-256
`0bad461d93041fdb21d284b0ae1684d2268adfb0f9a304f9b97ab4cd39900564`). The
byte-exact task copy and CPU-r2-only amendment receipt are bound in the status
and manifest. The approval uses root thread
`01a07029-b0e2-71d2-af08-ecd9aacc645f` and A2 thread
`01a07061-9c25-7e13-bcac-12d57c41c666`; it authorizes implementation and
guarded CPU r2 compilation/verification only.

The current blocker is corrected compilation and verification: bind the
namespace-correct exclusion manifest, verify zero overlap for all 624 prior
Alpaca development keys, and retain a fresh guarded CPU receipt. Capture
qualification and any substantive fits remain downstream gates.


The first bounded opened-fixture equivalence attempt on 2026-09-07 exited 1 after 1.0 seconds before model, E, H, or observation payload load. It was excluded for a support-digest namespace mismatch; the failure receipt is `experiments/TRR-0010/evaluation/opened_fixture_zero_delta_v1_failure/failure.json`. A CPU-only provenance check then derived B0 support from public `token_ids` and `attention_mask` only: 124,371 post-BOS rows, 17,126 supported IDs, exact equality with the stored support vectors, and published TRR-0009 digest `8090b923…6969d` (TRR-0010's distinct digest is recorded separately). No truth, accuracy, fitting, or capture was accessed; retry requires a fresh lease.


The corrected opened-fixture diagnostic v2 passed on the exclusive RTX 5080 lease (child exit 0; 6.325812 seconds). Across the first two already-opened public-base records in each Pile and Finance domain, zero-Delta merged-E, base-only state reload, and effective-readout reload were all exact with maximum absolute difference 0.0. Peak CUDA reserved memory was 2,871,001,088 bytes; peak RSS was 3,679,838,208 bytes; minimum host available memory was 18,749,104,128 bytes. The 1,050,674,192-byte effective-readout export and 29,390,316-byte base-only export are bound by hash but remain outside Git. No fit, truth, label, or accuracy operation occurred.

## Current publication and compatibility state

Draft PR [#20](https://github.com/A-lan-Z/Token-Reconstruction-Research/pull/20)
is open and unmerged against `task/TRR-0009`; the exact submitted body and
receipt remain bound under `experiments/TRR-0010/planning/`. The qualified B1
manifest metadata is now bound by the compact receipt and external manifest
hash; raw activation shards remain local and uncommitted.

The combined capture descriptor and public-support descriptor are bound, and
stage-1 B8x192 capture qualification passed. B0 has now completed its 13,000
updates cleanly, B1 expanded-fixed also completed cleanly, and current directional
training has completed with deployment export/recovery pending; expanded
directional training has not launched. The route-specific cost adapter is
synthetic-tested but has no production prediction timing artifact yet. The
A2 validation audit confirms exact 384-record public-base labels/masks/positions/H
hashes without opening final truth. Final evaluation, truth, and all quality
results remain pending.
