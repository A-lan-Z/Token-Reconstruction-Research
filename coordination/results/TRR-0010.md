# TRR-0010 — Preparation checkpoint

The directional readout is implemented and passes nine synthetic tests, including exact predictions after reloading the exported readout. The shared contract is a reviewed proposal, not an agreed execution contract. **No new reconstruction accuracy result exists: fitting, public activation capture, and fresh evaluation have not started.**

Implementation is saved locally at `a4985c0cd373c26d4361ce1c861fa35bdb25b10e`, based on published TRR-0009 `4602f98eeb03ac121d8bbc230d7e2b219551b914`. Tests ran before that commit against the exact source hashes recorded in their receipts. No PR was merged, global coordination state or active registry changed, or other agent workspace modified.

The proposed experiment compares current and expanded fitting banks, each with a fixed or trainable directional readout. All arms start from the same published `continued_fixed_readout` selected state; the unchanged state remains a reference. Agent 2 owns the banks, common training runner, and fixed-control fits. Agent 1 owns the directional fits and combined evaluation. No control fits have been duplicated.

The implementation learns independent additive readout rows for token IDs supported by the fitting bank. Unsupported rows remain at the public initialization. Prediction uses the current activation and the full vocabulary. Training and deployment use one materialized readout; the sparse scoring path is diagnostic-only. The deployed exported readout replaces the public embedding table. There is no A2 fallback.

The final nine-test receipt covers nonzero directional updates, gradients, unsupported-row invariance, state serialization, and exact effective-table/logit/token-ID equality after exported-readout reload. The common-hook smoke is bound to Agent 2 adapter commit `0aee09c4abb8d65b6ed3a42dc8ae93789509a518`; an end-to-end common training run remains outstanding.

Removing an unnecessary full-table copy gives conservative planning estimates of 6,860,603,392 bytes (6.389 GiB) at current support and 12,322,865,152 bytes (11.477 GiB) at full-vocabulary support. These are estimates, not measured GPU qualification. Actual expanded-bank support, optimizer scratch policy, live resources, and measured safety margin must be agreed before execution.

The corrected count-only inventory leaves 236 eligible Pile and 3,867 eligible Finance records after the opened TRR-0009 panel and the union of approved P08 reservations are excluded. This supports the proposed common 128-source-per-domain panel, paired across two target conditions. No records have been selected. The attempted Pile range extension failed the published guard and is excluded from capacity claims. Earlier pre-TRR-0009 counts remain preserved as superseded diagnostics.

The v4 contract proposes one exposure-based update count shared by all four arms, one public-only validation panel and checkpoint grid, one final natural panel, one A1+A2 comparator on the same declared denominator, and fixed practical quality/cost criteria. Exact-record uncertainty uses conservative paired-discordance Clopper-Pearson bounds. Degenerate token-bootstrap uncertainty and invalid ratio denominators cannot establish a successful route. The contract, precise bank bindings, and measured wall/disk/host/device limits still require joint agreement.

## Current blocker and next work

Automatic approval review rejected transmission of contract details, public resource hashes, source reservations, and scheduling metadata to Agent 2 thread `01a07061-9c25-7e13-bcac-12d57c41c666` because destination ownership/trust was not established. Destination-specific user approval was requested and remains pending. A notification-only message succeeded; Agent 2 acknowledged the block and has not retrieved the rejected payload. No alternate publication or message route was used to bypass it.

Next: obtain that permission, agree the common contract with Agent 2, bind the shared bank/validation/schedule artifacts and common runner, qualify resources under an agreed compute schedule, run the four owned fits, freeze all contenders, and perform one common evaluation with prediction-integrity checks before opening truth. The scientific report must then identify the best usable decoder, separate data and readout effects, and quantify the quality/cost gap to A1+A2. No direction-selection conclusion is justified yet.

Structured evidence: `experiments/TRR-0010/manifest.json`. Current proposal: `experiments/TRR-0010/planning/shared_contract.proposal.json` and `.md`. This checkpoint is local and unpublished.
