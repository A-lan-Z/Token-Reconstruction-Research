# TRR-0013 — Focused public practice versus ordinary continuation

Status: fitting in progress; final evaluation has not been scored. This is a new-weight continuation of the preserved TRR-0012 replication B1, not the lost PR20 checkpoint. No replacement or canonical benchmark claim is made.

The original public B1 fitting bank is effectively memorized: 21 errors in1,243,710 scored positions (99.9983% token accuracy), with99th-percentile classification loss0.00567. The rule fixed before this diagnostic therefore selected the additional-pool branch. A modest natural correction pool supplies1,419 errors in387,348 positions (99.6337%), with99th-percentile loss0.38159. These are fitting-side findings, not fresh reconstruction results.

Both continuations use the same14,048-record public pool, the same rebuilt B1 state, fixed normalized public embeddings, the unchanged residualMLP512/physical diagonal architecture, ordinary full-vocabulary classification loss, and6,000 updates of512 token draws. Identical eight-record batches are used. The focused arm retains half the ordinary token draws and draws the other half from the initial-loss top quintile within that batch, with replacement. No importance correction is applied: this deliberately changes the empirical CE weighting.

The reference arm exposes1,383,154 unique token positions; the focused arm exposes1,115,611. Both make3,072,000 draws and visit13,603 unique records. Maximum token repetition is11 versus33. Correction-pool draws are698,780 versus772,666, so the intervention changes the mix of token positions even though record batches are identical. Difficulty is frozen before training and uses legitimate public labels only. Support diagnostics count tokens in the actual combined fitting bank.

The prospective evaluation uses512 new Pile and256 new Finance sources, paired across public-base and historical synthetic-LoRA targets:768 independent sources and1,536 source/target observations. The declared128-token clip includes BOS, and exact recovery means all127 post-BOS tokens. A1+A2 uses a common first64 sources per domain per target, a separate128-independent-source denominator. P12 source ranges and published opaque exclusions were respected; no P03 holdout was read.

The primary contrast is focused minus ordinary on Pile/public-base exact-clip rate. A useful result requires at least+5 percentage points, a conservative paired interval lower bound above zero, no increased Pile token errors, nonnegative LoRA Pile exact change, and Finance token safeguards. The same rule evaluates ordinary versus frozen B1 secondarily. Thresholds, source quotas, model selection, and sampler were committed before new source selection. The precision check shows that a five-point effect can remain inconclusive with substantial gain/loss churn; lack of significance will not be called equivalence.

Ordinary continuation completed6,000 updates in264.36seconds, including cache loading, public validation, checkpoint writing, and checks; native updates took233.41seconds. The shared validation rule selected update4,000, with Pile exact recovery104/128 versus95/128 at update0. The focused arm and final independent results are pending. This validation improvement is not a method-success claim.

The synthetic fit/save/restore/predict/freeze/score check passed. The production gate was tested against missing methods/cells, source reordering, modified code, wrong dtype/shape, and invalid token IDs; each failed before truth access. Public capture preserved the native8×192 geometry and passed an exact output check. The full-size20-step qualification used2.55GiB GPU memory and12.1GiB host RSS. Every selection-grid model and optimizer is saved persistently and model tensor bytes are verified through the strict loader.

One failed diagnostic attempt is preserved: classification received int32 labels and failed before completing its first eight-record batch. The correction matches native training by converting CE labels to int64. No failed model state is eligible for selection. Tokenizer warnings on very long source documents are harmless to this experiment: source identities use the complete public record, while the model receives at most192 tokens.

Reproduction: `experiments/TRR-0013/README.md`; structured evidence: `experiments/TRR-0013/manifest.json`; prospective contract: `experiments/TRR-0013/contract.json`. Final model package, paired results, matched timing, and the three-way research decision will be appended after frozen evaluation.
